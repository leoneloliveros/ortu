<div class="card" style="margin-top: 20px;">
  <div class="card-header">
    Comentado por <%= comment.user.name %> hace <%= distance_of_time_in_words(comment.created_at, Time.current)%>
  </div>
  <div class="card-body">
    <p class="card-text"><%= comment.text %></p>
  </div>
</div>